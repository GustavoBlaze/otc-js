import GameFeatureManager from "~/client/GameFeatureManager";
import MessageModeManager from "./MessageModeManager";

export const g_feature = new GameFeatureManager();
export const g_messageMode = new MessageModeManager();
